import React from 'react'

export default function UserOutput({username }) {
    //creating a  UserOutput element
  return (
    <div>
        <p>{username} is the most gentle boy in class</p>
    </div>
  )
}

